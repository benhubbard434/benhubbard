@AGENTS.md

# Deploy rules
This project deploys to Netlify on every push to master. Never run `git push` autonomously. Commit freely, then tell the user what's pending and wait for an explicit signal ("push", "ship it", "deploy", "go live") before pushing.
